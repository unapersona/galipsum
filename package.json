{
  "name": "galipsum",
  "version": "1.0.0",
  "description": "Xerador de textos de recheo en galego",
  "scripts": {
    "styles": "node-sass --output-style compressed --source-map true --output src/assets src/assets",
    "scripts": "esbuild src/assets/script.js --bundle --outfile=src/assets/script.min.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cash-dom": "^8.1.0",
    "chota": "^0.8.0",
    "clipboard": "^2.0.8",
    "node-sass": "^6.0.0"
  }
}
